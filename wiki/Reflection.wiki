Reflection is a WordPress plugin which applies reflection effect to images with 'reflection' as one of the HTML attribute class values.

This plugin uses [http://raphaeljs.com/ Raphael] JavaScript library to create the reflection effect. It only supports Firefox 3.0+, Safari 3.0+, Opera 9.5+, and Internet Explorer 6.0+. On other browsers, you'll only see the image without the reflection.

[http://blog.qoqoa.com/2008/12/06/testing-reflection-plugin/ View Demo]
[http://wordpress.org/extend/plugins/reflection/ Download]

==Installation==
  # Unzip reflection-version.zip to wp-content/plugins/ .
  # Go to 'Manage Plugins' page and activate Reflection plugin.

==Usage==
  * If you're using WordPress rich editor to add an image to your blog post, you only have to add *reflection* to the image class value via Advanced Settings tab on an image edit popup. Note that class values are space separated.
  * If you're typing up the HTML manually, then there are several rules you must follow:
    # img tag class attribute should have *reflection* value
    # img tag must have width and height attributes.
    For example:
{{{
<p>
  <img class="alignnone reflection" src="blah.jpg" width="130" height="130"/>
</p>
}}}
{{{
<div>
  <img class="foo reflection bar" src="foobar.jpg" width="380" height="285"/>
</div>
}}}
  * You can configure gradient reflection via admin -> Settings tab -> Reflection menu.

==Screenshots==
Default reflection

http://bitpress.googlecode.com/svn/wiki/images/reflection_default.jpg

Gradient reflection (best on a dark background)

http://bitpress.googlecode.com/svn/wiki/images/reflection_gradient.jpg

Plugin settings page on WordPress admin

http://bitpress.googlecode.com/svn/wiki/images/reflection_admin.jpg

==Reflection Types==
  * Default reflection
    This reflection is a direct mirror of the original image, with .5 opacity. The reflection height is identical to the original image.
  * Gradient reflection with pixels height
    When you specify pixels gradient reflection height on the settings page, the reflection will take the specified pixels value. This reflection height is always the same between all reflected images on the blog.
  * Gradient reflection with percentage reflection height
    When you specify a percentage gradient reflection height on the settings page, the reflection will take a specified percentage of the original image. This reflection height differs between images, depending on the height of each original image on the blog.

==Changelog==
  * 0.2
    * Added auto div wrapper insertion. Removed div / p parent tag requirement.
  * 0.1
    * Initial release.